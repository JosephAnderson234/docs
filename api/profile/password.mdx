---
title: "Change Password"
sidebarTitle: "Change Password"
description: "Change the authenticated user's password. Requires the current password and a new one that differs from it."
url: "api/profile/password"
---

Use this endpoint to change your own password. You must supply the **current** password for verification, plus the new password. The new password must be 8–72 characters and **different** from the current one.

## Endpoint

```text
PATCH /profile/password
```

## Authentication

`Authorization: Bearer <accessToken>` — **required**. Any role (`ADMINISTRADOR` or `TRABAJADOR`) may change their own password.

## Request Body

<ParamField body="currentPassword" type="string" required>
  The user's current password. Verified against the stored hash.
</ParamField>

<ParamField body="newPassword" type="string" required>
  The new password. Must be **8 to 72 characters** and **must differ** from `currentPassword`.
</ParamField>

### Request example

```http
PATCH /profile/password
Authorization: Bearer <accessToken>
Content-Type: application/json
```

```json
{
  "currentPassword": "actual123",
  "newPassword": "nuevaSegura456"
}
```

## Response

### `204 No Content`

Returned on success. The response has no body. The current session remains valid and you can continue using your existing access token.

## Errors

| Status | `kind` | When |
| --- | --- | --- |
| `400 Bad Request` | `validation` | `currentPassword` is incorrect, `newPassword` equals `currentPassword`, or `newPassword` fails length validation (must be 8–72 characters). |
| `401 Unauthorized` | `unauthorized` | No token provided, the token has expired, is invalid, or belongs to a session that was replaced by a newer login. |

```json
{
  "statusCode": 400,
  "error": "Bad Request",
  "kind": "validation",
  "message": "La contraseña actual es incorrecta.",
  "path": "/profile/password",
  "timestamp": "2026-06-10T10:30:00.000Z"
}
```